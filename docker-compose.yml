version: '3.8'

services:
        python:
                build: .
                container_name: disaster-tweets
                ports:
                        - "8888:8888"
                volumes:
                        - type: bind
                          source: .
                          target: /home/jovyan/work

                working_dir: /home/jovyan/work
